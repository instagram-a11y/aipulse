import React from 'react';
import { renderHook, act } from '@testing-library/react-hooks';
import { useChat } from '@ai-sdk/react';

// Wait, testing react hooks in node requires a lot of setup. I'll just check the types.
